.container
  %h2.mb-3
    編集 #{resource_name.to_s.humanize}
  = form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f|
    = render "devise/shared/error_messages", resource: resource
    .field.mb-3
      = f.label :email, class: 'form-label'
      %br/
      = f.email_field :email, autofocus: true, autocomplete: "email", class: 'form-control'
    - if devise_mapping.confirmable? && resource.pending_reconfirmation?
      %div
        Currently waiting confirmation for: #{resource.unconfirmed_email}
    .field.mb-3
      = f.label :password, class: 'form-label'
      %i (変更しない場合は空白のままにしてください)
      %br/
      = f.password_field :password, autocomplete: "new-password", class: 'form-control'
      - if @minimum_password_length
        %em
          = @minimum_password_length
          文字以上
    .field.mb-3
      = f.label :password_confirmation, class: 'form-label'
      %br/
      = f.password_field :password_confirmation, autocomplete: "new-password", class: 'form-control'
    .field.mb-3
      = f.label :current_password, class: 'form-label'
      %i (変更を確認するために、現在のパスワードが必要です)
      %br/
      = f.password_field :current_password, autocomplete: "current-password", class: 'form-control'
    .actions.mb-3
      = f.submit "更新", class: 'btn btn-primary'
  %h3 アカウントを削除
  %div.mb-3
    削除しますか？ #{button_to "削除", registration_path(resource_name), data: { confirm: "本当に削除しますか？", turbo_confirm: "本当に削除しますか？" }, method: :delete, class: 'btn btn-danger'}
  = link_to "戻る", :back
