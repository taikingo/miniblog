.container
  - @posts.each do |post|
    = link_to post_path(post) do
      .card.mb-3
        .card-body
          %p
            = post.content
          %p
            = post.display_created_at

  .d-flex.justify-content-end
    = link_to new_post_path do
      .btn.btn-primary
        %div 新規作成